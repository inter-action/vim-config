
my zsh, tmux config repo

```bash
cp ./.tmux.conf ./.tmux.conf.local ./.zshrc ~
cp ./.gitconfig ~
```


# brew

```bash
brew install fzf neovim fd go uv gh jq tmux tree-sitter starship
brew install alacritty font-jetbrains-mono-nerd-font --cask
```